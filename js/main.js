document.addEventListener("DOMContentLoaded", () => {

  /* Section Renderers */
  renderSkills();
  renderProjects();
  renderPapers();
  renderVentures();

  /* Global UI */
  initReveal();
  initCTC();
  initScrollProgress();
});
